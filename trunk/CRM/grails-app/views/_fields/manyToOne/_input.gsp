<g:select name="${property}.id" from="${type.list()}" optionKey="id" optionValue="${type.getDefaultPropertyName()}" value="${propId? type.get(propId).id : ''}" noSelection="${allowNull == true ? ['null': message(code: 'default.select.one.label', default: 'Select One...')] : null}" class="form-control one-to-many"/>
<g:link class="add" controller="${propName? propName : property}" action="create">MI</g:link>
